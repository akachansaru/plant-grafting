using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LeafComponent : PlantComponentFrontEnd {
   

    //public override void GraftTo(PlantComponent component) {
        //if (
   //     Debug.Log("Grafting to " + component.GetType());
   // }
	
	// Update is called once per frame
	void Update () {
		
	}
}
